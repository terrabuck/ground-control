{
  "name": "ground-control",
  "version": "0.0.6",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . --icon=src/se.ico --out dist --asar=true --ignore=\"(config\\.json|\\.vscode|\\.git(ignore|modules)|\\.eslintrc\\.json)\"",
    "build-all": "electron-packager . --all --icon=src/se.ico --out dist --asar=true --ignore=\"(config\\.json|\\.vscode|\\.git(ignore|modules)|\\.eslintrc\\.json)\""
  },
  "author": "StreamElements",
  "license": "MIT",
  "devDependencies": {
    "electron": "^1.6.2",
    "electron-packager": "^8.5.2"
  },
  "dependencies": {
    "getmdl-select": "^1.0.4",
    "material-design-lite": "^1.3.0"
  }
}
